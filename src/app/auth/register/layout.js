import '../../globals.css'

export const metadata = {
  title: 'Daftar | Vascomm',
  description: 'Login page',
}

export default function LoginLayout({
  children,
}) {
  return (
    <div>
      {children}
    </div>
  )
}
