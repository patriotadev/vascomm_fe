import '../../globals.css'

export const metadata = {
  title: 'Masuk | Vascomm',
  description: 'Login page',
}

export default function LoginLayout({
  children,
}) {
  return (
    <div>
      {children}
    </div>
  )
}
